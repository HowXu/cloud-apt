# Worker & client build
node_modules/
apt-worker/dist/
apt-client/dist/
.wrangler/

# Tests
coverage/

# Local repo runtime data
# conf/distributions.template 模板
# conf/distributions live 副本
local-repo/conf/distributions
# GPG
local-repo/keys/
# Local catalog (synced from worker, regenerable)
local-repo/packages.json
# Release/Packages (regenerable from worker)
local-repo/dists/
# Pre-migration leftovers (kept only so old users can run reprepro-to-catalog.sh)
local-repo/db/
local-repo/pool/
local-repo/incoming/
local-repo/.publish/
local-repo/.repository.lock
local-repo/config.env
local-repo/cloud-apt-export-*.tar.gz
local-repo.bak.*/
__pycache__/
local-repo/scripts/*.sh.bak

# Example package build outputs
example/artifacts/
example/hello
example/cloud-apt-hello

# OS / Editor
.DS_Store
*.swp
.idea/
.vscode/

# Secrets
*.env
.env
apt-worker/.dev.vars

.superpowers/*
docs/*
AGENTS.md
